services:
  db:
    image: postgres:15
    container_name: my-stock-db
    environment:
      POSTGRES_USER: aaron
      POSTGRES_PASSWORD: password123
      POSTGRES_DB: trading_db
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: